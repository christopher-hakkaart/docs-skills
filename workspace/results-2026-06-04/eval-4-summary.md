- Classified as **Task** (page title supplies the H1, so the title is the H1 and body sections start at H2). The input mixed task steps with a Concept lead, reference-style Notes, and unrelated upgrade/diagnostic sentences; grouped them into a concept lead, a `Prerequisites:` list, the steps, and a reference `Notes` section.
- Renamed title from `Configuring The Compute Environment` to `Configure the compute environment` (active verb + noun, sentence case). **Anchor change:** the title/H1 anchor changes; update inbound links. Also reshaped `## Step 1: Log In` (bold, Title Case) into `## Sign in and launch a run` and demoted nothing — fixed the skipped heading level (`#### Notes` H4 → `## Notes` H2). Flag these renamed headings for inbound-anchor updates.
- Terminology fixes (7): `Tower`/`the platform` → Seqera Platform (2), `NextFlow` → Nextflow, `multiQC` → MultiQC, `Launch Pad` → **Launchpad**, `data explorer` → **Data Explorer**, `runs tab` → **Runs** tab; lowercased feature nouns `Credentials`, `Compute Environments`, `Workspace`.
- Pipeline vs workflow / run vs task (2): "The workflow failed to launch" → "a pipeline fails to launch" (a Platform pipeline, not Nextflow code); "Each job ran on its own node" → "Each task runs on its own node".
- Bold vs backticks (5): `Launch` button → **Launch**; removed bold from the `Step 1` heading; kept `TOWER_ACCESS_TOKEN`, `--profile`, `nextflow.config` in backticks.
- Replaced the vague performance claim with the supplied facts: "significantly faster and supports various providers, which should improve things for a number of users" → "reduces run-submission p99 latency from 12s to 4s and adds support for Google Cloud Batch, joining AWS Batch and Azure Batch".
- Style/voice swaps (style guide): `utilize` → use (2), `Click` → select, `Since` → removed/`Without`, `can not` → cannot; fixed passive "is submitted to AWS Batch by Tower" → "Seqera Platform submits the job to AWS Batch"; fixed future tense ("will be generated", "should be run", "before the upgrade is performed") → present/imperative; cut the chatty "don't sweat it — it's not rocket science", "It is recommended that", and "You will utilize these settings later" (Notes is reference material: long forms, no contractions).
- Clarity: defined the executor on first use; split the 42-word credential-permissions sentence into a prerequisite; broke the noun string "project integration custom settings" → "custom settings for project integrations".
- Replaced fake credentials with placeholders: `sk-1a2b3c-real-looking-secret` → `<your_access_token>`, `admin@acme-corp.com` → `admin@example.com`.
- Reduced length roughly 25% (about 235 → 180 words).
- **Flagged for review:** glossary candidates — **executor** and **compute environment** recur and may warrant glossary entries. The migration/upgrade line and the launch-failure diagnostics read as separate procedures mashed into this task; kept them but confirm they belong on this page.
