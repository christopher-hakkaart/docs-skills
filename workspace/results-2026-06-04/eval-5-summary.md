- Classified as four typed sections and split accordingly: a **Concept** (`Workspaces`) plus a **Task** (`Create a workspace`), a **Reference** (`Compute environment settings`), a **Troubleshooting reference** (the `AccessDenied` error), and a **Tutorial** (`Run nf-core/rnaseq on AWS Batch`). The original mashed all of these onto one page.
- **Overview → Concept + Task split:** renamed the non-specific `Overview` to the noun `Workspaces`; replaced the vague definition ("Workspaces organize your work") with a concrete one (groups pipelines, compute environments, and credentials for a team). Lifted the embedded steps into a separate `Create a workspace` Task (active verb + noun), converting the inline "1) you navigate... 2) you should click..." prose into a numbered imperative list and the "make sure you have an organization" sentence into a `Prerequisites:` list. Added the missing final step (select **Add**) supplied in the facts. Used **New**/**Add** bold UI labels and `select` instead of `click`.
- **Cut the `Summary` section** ("That's how workspaces work!") — a restatement with no content.
- **Settings → Reference:** renamed `Settings` to the noun `Compute environment settings`; converted the prose lines into a 4-column table (Setting | Type | Default | Description). Cut the prose narrative between fields ("It's really important to choose this carefully", "As mentioned, this matters for cost"). Filled defaults from facts (`region`/`workDir` no default, `maxCpus` 256).
- **Launch problems → Troubleshooting reference:** moved the actual error into the title with `Error:` prefix and backticks (`AccessDenied (Service: S3, Status Code: 403)`). Cut the chatty opener ("Don't worry!") and the hedge ("might be due to a variety of factors"); replaced with a named cause and a `resolve` step list (grant `s3:PutObject`, relaunch).
- **Walkthrough → Tutorial:** shortened the title-case, hyphen-separated `Pipeline Walkthrough - A Complete And Exhaustive End To End Guide To Everything` to `Tutorial: Run nf-core/rnaseq on AWS Batch` (sentence case, `Tutorial:` prefix, active verb). Cut the inflated opener ("In today's fast-paced world, let's embark on an exciting journey... transform how you work!"). Added a goal-stating lead with the outcome (completed rnaseq run), a `Before you begin` prerequisites section, and active-verb task subsections. Cut the orphan `More info`/"See the other page" link-only section.
- Body starts at H2 — the page title is supplied by the tutorial/section titles; removed the H1 `# Overview`.
- Net length reduced roughly 30% of original prose; the bulk of removed words were marketing/chatty filler and the two restatement sections.
