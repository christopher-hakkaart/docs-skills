- Classified as a mix of **Concept** (token handling, schema cache, retry behavior), **Reference** (the three run-characteristic facts), and **Task** (`Configure a run`). Split the single slop blob into four typed sections.
- Broke 9 formulaic AI structures: binary contrast/negative listing ("This isn't a bug. It's a design decision. Not slow. Not broken. Just misunderstood."), self-posed rhetorical question ("Why does retry behavior matter?"), anaphora ("It retries fast. It retries quietly. It retries every time."), dramatic fragmentation ("That's it. That's the feature.", "Speed. That's it."), "Despite these challenges..." formula, listicle-in-a-trench-coat ("The first wall... The second wall... The third wall..."), false agency ("the run decides when to fail and the metrics tell us why"), dead metaphor/false agency ("Like a conductor leading an orchestra, the platform orchestrates everything"), and historical-analogy stacking ("AWS didn't solve this. Google didn't solve this. Stripe didn't solve this.").
- Cut 2 fractal/self-referential framings ("In this section, we'll cover three benefits.", "As discussed in this section...") and the superficial participle phrase ("underscoring its role as a critical component").
- Fixed sentence joins and punctuation: split 2 ", so" connectors (token expiry, schema cache), removed 1 semicolon and 1 mid-sentence colon ("There's one rule: keep tokens short-lived").
- Removed bold-first bullets (**Performance** / **Reliability** / **Security**) and the `→` Unicode arrow; rewrote the three facts as plain bullets per the keep-the-three-facts instruction.
- Cut marketing tricolons and one-point dilution ("Resilient, reliable, robust pipelines"; "Fast runs save time. Quick runs save effort. Speedy runs help everyone."; "So performance is fast. Now reliability is high. But security is strong.") and the lazy extreme ("Every user always wants faster runs — nobody wants to wait.").
- Renamed the section title from `Token handling.` to `Token handling` (removed terminal period); converted the `--profile` flag and config items from quoted/prose form into a `Configure a run` Task with a numbered steps list, flags in backticks, Oxford comma added ("region, work directory, and max CPUs").
- Added the supplied facts the original omitted: exponential backoff starting at 30 seconds, the vCPU-quota cause of run failures, and the pre-submit quota check that queues the run. Token expiry corrected to "1 hour"; schema cache requires a restart after changes.
- Reduced length roughly 65% (about 250 to about 90 words).
- Renamed heading flag: the section anchor changed from `#token-handling.`-style is unaffected, but the new `## Retry behavior`, `## Run characteristics`, and `## Configure a run` headings are new anchors; update any inbound links if this content previously lived under a single anchor.
