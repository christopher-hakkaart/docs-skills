# Change summary

- **Seqera Platform** · classified as **Concept** (run behavior — retries, quota, token handling — with a short config list at the end). Kept as one section; no re-typing needed.
- **False drama (`structures.md`):** removed the binary contrast ("This isn't a bug. It's a design decision."), the negative listing ("Not slow. Not broken. Just misunderstood."), the dramatic fragmentation ("That's it. That's the feature."), the self-posed rhetorical questions ("Why does retry behavior matter?", "The result?"), the anaphora ("It retries fast. It retries quietly. It retries every time."), and the tricolon ("resilient, reliable, robust"). Replaced with the actual retry behavior: 3 automatic retries with exponential backoff from 30 seconds.
- **Formulas and false agency (`structures.md`):** removed the "Despite these challenges" dismissal, the false range ("From small scripts to enterprise empires"), the false agency ("the run decides", "the metrics tell us"), the listicle-in-a-trench-coat ("The first wall... second... third..."), the superficial participle ("underscoring its role as a critical component"), the invented concept label ("the observability gap"), the dead metaphor (conductor/orchestra), and the historical analogy stacking (AWS/Google/Stripe). Stated the real quota-check behavior instead (checks vCPU quota before submitting, queues if exceeded).
- **Markdown and rhythm (`structures.md`):** dropped the bold-first bullet leads (**Performance**/**Reliability**/**Security**) that carried no information, removed the decorative em-dashes ("— yes, automatically —") and the Unicode arrow (→), cut the fractal summary ("In this section, we'll cover" + "As discussed in this section"), removed the "So/Now/But" paragraph-opener crutches, replaced lazy extremes ("Every user always", "nobody", "everyone") with specifics, and collapsed the one-point dilution ("Fast runs... Quick runs... Speedy runs...") into a single statement.
- **Connectors and punctuation (`structures.md`):** split the ", so" join ("expires after an hour, so refresh it") and the ", which means" join ("cached, which means changes need a restart") into separate sentences; replaced the semicolon and the mid-sentence colon ("There's one rule: keep tokens short-lived") with periods; removed the terminal period from the heading ("Token handling." → "Token handling"); added the Oxford comma ("region, work directory, and max CPUs"); made the config-list punctuation parallel (no terminal periods); and put the flag in backticks (`--profile`) rather than quotes.
- **Terminology (`terminology.md`):** replaced "the platform" (meaning the product) with **Seqera Platform**.
- Reduced length roughly 65% (~230 → ~110 words).

**Flagged for review (never-invent rule):** the original listed three failure "walls" — quota, networking, and logging. Only the vCPU-quota cause is in the supplied facts, so the networking and logging causes were dropped rather than invented.

**Structure (step 6):** no prerequisites and no inline troubleshooting content (no error strings, symptom → cause → fix entries, or "If you see…" recovery) are present, so the `docs-structure` handoff has nothing to reformat or move.
