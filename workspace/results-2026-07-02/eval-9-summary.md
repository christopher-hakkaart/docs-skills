- **Nextflow** (open-source, not Seqera Platform) · the page mixed **Concept** (what Nextflow is, the `workflow` block) with **Task/Reference** (how to run and resume pipelines) and a **Troubleshooting** entry; kept them on one page with the run instructions as the primary content.
- Renamed the title from `Running NextFlow Pipelines` to `Run Nextflow pipelines` (active verb + noun, per Task rules; corrected the `NextFlow` → `Nextflow` spelling). Note: renaming the heading changes its anchor — repoint any inbound links to `#running-nextflow-pipelines`.
- Cut the marketing and slop: `powerful`, `cutting-edge`, sales verb `empowers` → dropped, `seamlessly`, the self-referential opener `In this section, we'll explore`, the hedge `It's worth noting that`, and `utilize` → `use`.
- Applied the Nextflow product reference: corrected the single-vs-double-dash convention and moved the flags into backticks — `-input` → `--input <path>` (pipeline parameter, two dashes), `--resume` → `-resume` (core option, one dash), and the `"profile"` setting → `-profile` (core option, one dash); backticked `nextflow run`.
- Corrected `pipeline block` to the `workflow` block (Nextflow DSL keyword in backticks, not a Platform "pipeline") and converted the run/flag prose into a scannable list.
- Reformatted the troubleshooting entry per the core rules: dropped the chatty opener (`Sometimes things go wrong`, `don't worry`) and the hedge (`a variety of factors`), moved the error into an `Error:` heading with the message in backticks, and restructured the body as symptom → cause → resolution (using the supplied cause and the `-resume` fix). **Left the `## Troubleshooting` section inline** — Nextflow has no troubleshooting destination in this repo (per the `docs-structure` handoff), so the entry stays on the page rather than moving off-page.
- Reduced length roughly 35% (about 120 → 80 words).
